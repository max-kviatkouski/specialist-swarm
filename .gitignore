# Secrets — NEVER commit (the ANTHROPIC_API_KEY lives here)
.env
.env.*
!.env.sample
!.env.example

# NOTE: session state (.coordinator_id, .specialist_ids.json, .skill_ids.json,
# .environment_id, .last_session_id) and outputs/ are intentionally COMMITTED for
# the demo so the partner can show the real deliverables and open the live session.
# These are resource IDs, NOT credentials. The API key stays only in .env (above).

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
